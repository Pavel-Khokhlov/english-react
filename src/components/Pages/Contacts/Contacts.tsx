import React from "react";
import FormMessage from "../../FormMessage/FormMessage";

import "./Contacts.sass";

function Contacts() {
  
  return (
    <section className="section">
      <h1 className="title title__main">Контакты</h1>
      <FormMessage />
    </section>
  );
}

export default Contacts;
